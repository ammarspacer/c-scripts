#include <stdio.h>
#include <string.h>

int main(){
	int  n, i, j, ;
	scanf("%d", &n);
	char arr[n][50];
	for(i=0;i<n;i++){
		scanf("%s", &arr[i]);
	}
	for(i=0;i<n;i++){
		cek = 0; //Mengecek keberadaan a
		while(j=0;j<strlen(arr[i])){
			if(arr[i][j] == 'a'){
				cek++;
				j++;
			}
		}
	}
	
	if()
	return 0;
}