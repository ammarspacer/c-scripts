#include "head.h"
/*Saya [AMMAR ASHSHIDDIQI] mengerjakan evaluasi [REMEDIAL] dalam mata kuliah 
[ALGORITMA DAN PEMROGRAMAN 1] untuk keberkahanNya maka saya tidak melakukan kecurangan seperti yang telah dispesifikasikan. Aamiin*/
void olah(int n,int angka[50]){
	int setarray1[50]; //array penampung set array 1
	int setarray2[50]; //array penampung set array 2
	int i; //iterasi
	for(i=0; i < (n/2); i++){
		setarray1[i] =  angka[i]; //proses  penampung set array 1
	}
	
	for(i= n/2; i <n ; i++){
		setarray2[i] = angka[i]; //proses  penampung set array 2
	}
	
	int cek = 0; //variabel pengecek
	
	/*Proses*/
	for(i=0;i<n;i++){
		if(setarray2[i] )
	}
	
	//hasil
	if(cek>=1){
		printf("valid");
	}
	
	
}
